import ReactMarkdown from "react-markdown";

export const Changelog = () => {
  return (
    <main className="bg-white dark:bg-blue-900 h-[calc(100vh-5em)] flex justify-center">
      <ReactMarkdown># Hello, *world*!</ReactMarkdown>
    </main>
  );
};
